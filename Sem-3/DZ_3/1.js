const a = Number.parseFloat(prompt("Введите температуру в градусах: "));

function sum (a)
    {
        return far = Math.round((9 / 5) * a + 32);
    }

alert (`Температура по цельсию: ${a} \nТемпература по Фаренгейту: ${sum(a)}`);