const a = Number.parseFloat(promt("enter num 1: "));
const b = Number.parseFloat(promt("enter num 2: "));
const s = sum(a,b);

alert (`Result summ ${a} + ${b} = ${s}`)

function summ(x,y){
    return x + y;
}